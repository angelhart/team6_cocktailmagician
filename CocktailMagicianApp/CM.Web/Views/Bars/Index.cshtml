@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "__AjaxAntiForgeryForm" }))
{
	@Html.AntiForgeryToken()
}

@model IEnumerable<BarIndexViewModel>

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/_Layout.cshtml";
}
@section Styles {
}




<head>
	<link href="//cdn.datatables.net/1.10.21/css/jquery.dataTables.min.css" rel="stylesheet">
</head>

<h1>World's Best Bars</h1>

<p>
	<a asp-action="Create">Create New Bar</a>
</p>

<table id="barsTable" class="display">
	<!--hover order-column stripe-->
	<thead>
		<tr>
			<th>Image</th>
			<th>Name</th>
			<th>Country</th>
			<th>City</th>
			<th>Address</th>
			<th>Avg. Rating</th>
			<th>Edit</th>
			<th>Delete</th>
		</tr>
	</thead>
</table>


<div>
	<a asp-action="Index">Back to List</a>
</div>
@section Scripts {

	<script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.21/js/jquery.dataTables.min.js"></script>

	<script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.10.21/js/dataTables.bootstrap4.min.js"></script>

	@*<script src=https://code.jquery.com/jquery-3.5.1.js></script>*@

	<script src="~/js/BarsDataTable.js"></script>

	@{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

}
@*<table class="table">
		<thead>
			<tr>
				<th>
					Image
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Name)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.Country)
				</th>
				<th>
					Address
				</th>
				<th>
					@Html.DisplayNameFor(model => model.AverageRating)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td>
						<a asp-action="Details" asp-route-id="@item.Id">
							<img src="@item.ImagePath" height="50" width="auto" />
						</a>
					</td>
					<td>
						<a asp-action="Details" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.Name)</a>
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.Country)
					</td>
					<td>
						@Html.DisplayFor(modelItem => item.City), @Html.DisplayFor(modelItem => item.Street)
					</td>
					<th>
						@Html.DisplayFor(modelItem => item.AverageRating)
					</th>

					@if (User.IsInRole("Magician"))
					{
						<td>
							<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
						</td>
						<td>
							<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
						</td>
					}
				</tr>
			}
		</tbody>
	</table>*@

